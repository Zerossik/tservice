export const selectIsContactsLoading = ({ contacts }) => contacts.isLoading;
export const selectContacts = ({ contacts }) => contacts.contacts;
export const selectTotalOrders = ({ contacts }) => contacts.totalOrders;
export const selectIsTableVisible = ({ contacts }) => contacts.isTableVisible;
